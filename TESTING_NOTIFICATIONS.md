# Testing Notification Broadcast - دليل الاختبار

## خطوات الاختبار Steps

### 1. شغّل الخادم المحلي (Start Dev Server)
```bash
cd c:\Users\pc\car-maintenance-tracker
npm run dev
```

### 2. افتح الكونسول في المتصفح (Open Browser Console)
اضغط `F12` في المتصفح لفتح أدوات المطور

### 3. انتقل لصفحة الإدارة (Navigate to Admin)
```
http://localhost:5173/admin/login
```

### 4. انتقل لتبويب "إعدادات النظام" (Go to Settings Tab)

###5. املأ نموذج بث الإشعارات (Fill Broadcast Form)
**جميع الحقول إلزامية**:
- ✅ عنوان الإشعار (مثال: "تحديث جديد")
- ✅ نص الرسالة (مثال: "تم إضافة مزايا جديدة")  
- ✅ نوع التنبيه (اختر من: تنبيه، تحذير، نجاح، أو خطأ)

### 6. اضغط "إرسال للجميع" (Click Send)

### 7. راقب الكونسول (Watch Console)
ستظهر رسائل مثل:
```
🔔 sendAnnouncement called with: {...}
✅ Validation passed, sending to admin store...
📡 Response from postAnnouncement: {...}
✅ Broadcast sent successfully!
```

### 8. تحقق من صفحة الإشعارات (Check Notifications Page)
انتقل إلى الرابط:
```
http://localhost:5173/notifications
```
يجب أن ترى الإشعار الذي أرسلته

## الأخطاء الشائعة Common Errors

### ❌ "جميع الحقول إلزامية"
**السبب**: لم تملأ أحد الحقول  
**الحل**: تأكد من ملء العنوان، الرسالة، ونوع التنبيه

### ❌ "فشل إرسال الإشعار"
**الأسباب المحتملة**:
1. مشكلة في صلاحيات قاعدة البيانات (RLS policies)
2. المستخدم ليس Admin
3. مشكلة في الاتصال بـ Supabase

 **الحل**: افحص الكونسول (`F12`) لمعرفة تفاصيل الخطأ
